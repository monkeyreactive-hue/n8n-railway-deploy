# Copy these into Railway Variables (never commit real secrets)
N8N_BASIC_AUTH_ACTIVE=true
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme
N8N_HOST=0.0.0.0
# N8N_PORT is set at runtime using the Procfile, mapped to Railway's $PORT
WEBHOOK_URL=https://<your-railway-subdomain>.up.railway.app/
EXECUTIONS_PROCESS=main
GENERIC_TIMEZONE=Asia/Kolkata
# DB & ENCRYPTION key should be set in Railway (see below)
